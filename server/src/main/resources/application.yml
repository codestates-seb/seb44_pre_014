spring:
    h2:
        console:
            enabled: true
            path: /h2
            settings:
                web-allow-others: true
    datasource:
        driver-class-name: org.h2.Driver
        url: jdbc:h2:mem:test
        username: sa
        password:
kakao:
    redirect-url: ${REDIRECT_URL}
    api-key: ${API_KEY}
    